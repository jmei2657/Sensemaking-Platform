Create or transfer over the  ``` manually_installed.txt ```  and the ``` run.sh ``` file on the instance 

Make the script executable: ``` chmod +x run.sh ``` 

Run the script: ``` ./run.sh ``` 

The script will print Done! when completed 
